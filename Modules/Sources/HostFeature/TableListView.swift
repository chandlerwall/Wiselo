import SwiftUI

// FIXME: Probably delete this view.

struct TableListView: View {
    var body: some View {
        Text("Hello, World!")
    }
}

#if DEBUG

import PreviewHelpers

struct TableListView_Previews: PreviewProvider {
    static var previews: some View {
        Preview {
            TableListView()
        }
    }
}

#endif
